import { styled } from "styled-components";

const FSTextBlock = styled.div`
  margin: 0 auto;
  color: #fff;
  font-size: 30px;
  font-weight: 400;
  line-height: 48px;
  padding-bottom: 65px;
`;

export default FSTextBlock;
