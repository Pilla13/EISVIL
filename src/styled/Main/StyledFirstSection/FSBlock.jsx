import { styled } from "styled-components";

const FSBlock = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 400px;
  margin: 0 auto;
`;

export default FSBlock;
